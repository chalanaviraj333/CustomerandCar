import { CustomerDataWithImages } from './customer-data-with-images';

describe('CustomerDataWithImages', () => {
  it('should create an instance', () => {
    expect(new CustomerDataWithImages()).toBeTruthy();
  });
});
